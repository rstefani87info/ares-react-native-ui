# Checklist Migliorie aReS React Native UI

- [1] 17. Rendere eseguibili test minimi del package (smoke test): render di `ApplicationRoot`, `ARESProvider`, un campo `Text`, e un componente output (es. `Modal`) con `react-test-renderer` o `@testing-library/react-native`.
- [2] 18. Aggiungere/standardizzare configurazione Jest (se necessaria) e assicurare che `yarn workspace @ares/react-native-ui test` sia stabile anche senza ambiente Android/iOS.
- [3] 19. Verificare che `eslint` non richieda toolchain native per girare e che le regole siano coerenti con il resto del monorepo.

